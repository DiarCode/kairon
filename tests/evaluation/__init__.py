"""Tests for the kairon.evaluation subpackage (cost calibration, etc.).

The package is intentionally side-effect-free. The tests pin the
W2.1 Almgren-Chriss ``eta`` calibrator so downstream test authors
can rely on it.
"""
